.DS_Store
tools/qlctool/.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# QLC+ writes this every 30 seconds while a workspace is open and deletes it
# on a clean exit. It is transient editor state, not the show.
*.autosave.qxw
.coverage
